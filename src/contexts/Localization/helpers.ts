// import { EN } from 'config/localization/languages' // not implemented

const publicUrl = process.env.PUBLIC_URL

export const LS_KEY = 'pancakeswap_language'

export const fetchLocale = async (locale) => {
  return ''
}

export const getLanguageCodeFromLS = () => {
  try {

    return ''
  } catch {
    return ''
  }
}

// Briws: deleted returns because Language not implemented in briws-uikit